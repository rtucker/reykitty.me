# The URL the site will be built for
base_url = "https://reykitty.me"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

theme = "emily_zola_theme"

taxonomies = [
    { name = "categories", feed = true},
    { name = "tags", feed = true},
]

title = "reykitty.me"

generate_feeds = true

author = "Rey T"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
# Put all your custom variables here
# Author name, appears in footer.
emily_author = "reykitty"

# Path to the header icon / favicon image.
# Please save image to static/images
emily_icon = "/images/reykitty.png"

emily_favicon = "/images/reykitty.png"

# number of posts in index.html.
emily_indexposts = 5